node_modules/
.next/
out/
dist/
build/
target/
.anchor/
test-ledger/
*.tsbuildinfo
.env
.env.local
.env.*.local
.DS_Store
.vercel

# Never commit a keypair. Not the program keypair, not the deploy wallet, not a
# throwaway devnet one — devnet habits become mainnet habits.
*.keypair.json
keypairs/
!keypairs/.gitkeep
